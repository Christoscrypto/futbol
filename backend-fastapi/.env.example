# Copia este archivo a ".env" (sin ".example") y pon tu cadena de
# conexión real de MongoDB Atlas (la misma que ya usabas con Netlify).
MONGODB_URI=mongodb+srv://christopher:<db_password>@cluster0.tohiuq0.mongodb.net/?appName=Cluster0
MONGODB_DB=dimacup

# Dominios permitidos por CORS, separados por coma.
# Deja "*" mientras pruebas; en producción pon tu dominio real, ej:
# ALLOWED_ORIGINS=https://dimacup.netlify.app,https://tudominio.com
ALLOWED_ORIGINS=*
